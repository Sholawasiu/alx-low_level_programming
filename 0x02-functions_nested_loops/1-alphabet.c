#include "main.h"

/**
*print_alphabet - function that print alphabet
*it prints in lowercase
*Return: no return
*
*/

void print_alphabet(void)
{
int n;
for (n = 'a' ; n <= 'z'; n++);
_putchar(n);
_putchar(10);
}

