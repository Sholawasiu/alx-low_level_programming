#include <stdio.h>
#include <stdlib.h>

/**
*main - Entry point
*Description: print all single digit number of base 10 starting from 0
*Return: Always (0)
*/
int main(void)

{

int ch;
for (ch = 48; ch <= 57; ch++)
putchar(ch);
putchar(10);
return (0);
}
